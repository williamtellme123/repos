#!/bin/bash
set -e

echo Hello
base_dir=$(dirname $0)
echo $base_dir


if [ $# -lt 1 ];
then
  echo "USAGE: $0 [-daemon] [-name servicename] [-loggc] classname [opts]"
  echo "This is then statement"	
  exit 1

else 
   echo "this is else statement"
fi


